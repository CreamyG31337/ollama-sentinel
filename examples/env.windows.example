# COPY to .env — see ../.env.example for keys.
# Windows: optional PAGING_POWER_W if you measured a card-specific paging threshold.
# Task Scheduler: ollama-sentinel --once --toast every 15 minutes.

# PAGING_POWER_W=200
